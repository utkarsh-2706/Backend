<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Users — Add</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-zinc-900 flex items-center justify-center p-6">
    <main class="w-full max-w-md">
        <% if (typeof success !== 'undefined') { %>
            <div class="mb-4 p-3 rounded bg-green-600 text-white text-sm">
                <%= success %>
            </div>
        <% } %>

        <% if (typeof error !== 'undefined') { %>
            <div class="mb-4 p-3 rounded bg-red-600 text-white text-sm">
                <%= error %>
            </div>
        <% } %>

        <section class="bg-white/5 backdrop-blur-sm p-6 rounded-lg shadow-lg">
            <h1 class="text-2xl font-semibold text-white mb-4">Add user</h1>

            <form action="/create" method="POST" class="space-y-4" novalidate>
                <div>
                    <label for="name" class="block text-sm text-gray-200 mb-1">Name</label>
                    <input id="name" name="name" type="text" required
                        class="w-full p-2 rounded bg-gray-50 text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-400"
                        placeholder="Full name" />
                </div>

                <div>
                    <label for="email" class="block text-sm text-gray-200 mb-1">Email</label>
                    <input id="email" name="email" type="email" required
                        class="w-full p-2 rounded bg-gray-50 text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-400"
                        placeholder="you@example.com" />
                </div>

                <div>
                    <label for="image" class="block text-sm text-gray-200 mb-1">Image URL</label>
                    <input id="image" name="image" type="tel" pattern="\d{10}" required
                        class="w-full p-2 rounded bg-gray-50 text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-400"
                        placeholder="abcd.png" title="Enter valid image URL" />
                    <p class="mt-1 text-xs text-gray-300">Digits only — 10 characters</p>
                </div>

                <div class="flex gap-3">
                    <button type="submit" class="flex-1 bg-blue-500 hover:bg-blue-600 text-white p-2 rounded">
                        Add User
                    </button>
                    <button type="reset" class="bg-gray-700 hover:bg-gray-600 text-white p-2 rounded">
                        Reset
                    </button>
                </div>
            </form>
        </section>
    </main>
</body>
</html>